<% layout("./layouts/basic.eta") %>

<main>
    <section>
        <p>Your nearest Deno Deploy region is... <span><%= it.location.name %></span> <code><%= it.location.id %></code></p>
        <p>This isolate has been live for <strong><%= it.time %></strong> seconds and has served <%= it.requests %> requests.
    </section>
</main>